# Team Task Assignment

## Task Summary
一句话说明本次任务。

## Assignee
- Member: `<member_name>`
- Role: `<role_name>`

## Objective
请完成以下目标：
1. `<目标1>`
2. `<目标2>`
3. `<目标3>`

## Stable Inputs
本次任务只能基于以下已验收共享工件开展：
- `<输入路径1>`
- `<输入路径2>`
- `<输入路径3>`

## Reference Materials
以下资料可参考，但不得覆盖已验收共享工件中的稳定事实：
- `<参考路径1>`
- `<参考路径2>`

## Expected Outputs
你的正式输出必须写入以下位置：
- `<输出路径1>`
- `<输出路径2>`

## Requirements
请确保输出满足以下要求：
1. `<要求1>`
2. `<要求2>`
3. `<要求3>`

## Out of Scope
本次任务不得越界处理以下内容：
- `<禁止项1>`
- `<禁止项2>`
- `<禁止项3>`

## Assumptions And Unknowns
如果发现信息不足，请遵循以下规则：
1. 不要硬编
2. 将内容写入“假设 / 待确认事项”
3. 明确标注影响范围
4. 不要擅自修改上游已验收结论

## Definition Of Done
满足以下条件才算完成：
1. 输出已写入指定共享工件
2. 内容与输入工件一致
3. 没有越界改动其他阶段结论
4. 可提交 reviewer 审查

## Completion Reply Format
完成后请按以下格式回复：
- Updated Artifacts:
  - `<路径1>`
  - `<路径2>`
- Key Changes:
  1. `<关键变更1>`
  2. `<关键变更2>`
- Open Questions:
  1. `<待确认项1>`
  2. `<待确认项2>`
- Ready For Review: `<yes/no>`
