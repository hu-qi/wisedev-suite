# 案例 01：上传下达模块需求规格片段

## 功能需求条目
### FR-001 工作调度指令下发
系统应支持巡办管理员创建调度指令并向一个或多个巡组发布，指令应包含标题、正文、截止时限、附件和反馈要求。

### FR-002 调度指令签收反馈
系统应支持巡组对收到的调度指令进行签收，并提交阶段反馈或办结反馈。

## 验收标准片段
- 发布成功后，目标巡组工作台应出现对应待办。
- 签收动作完成后，指令状态应变更为“已签收”。
